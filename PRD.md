# PRD.md —— Ja的小屋 · 第一版产品需求文档

> 项目：Ja的小屋（仓库名 `myself-block`）· 版本：**v1.6** · 日期：2026-09-24 · 作者：Jack（仓库账号 JIMIHA-WU）
> 本文是项目基础文档。写代码前的一切分歧，以本文为准；本文没写的，按第 9 节"默认值"处理。
> 上游依据：`research.md`（同类产品研究）· 约束依据：`AGENTS.md`（协作规则）
> v1.1 变更：按独立审查意见修订 8 处「必须修改」级问题（隐私表述、比例冲突、死链、验收项可判定性、缺失章节等）。
> v1.2 变更：第二轮复审后补 3 处（"风格一致"的判定口径、数字一致性验收、隐私自查验收），并细化封面比例与"关于"锚点。
> v1.3 变更：**清除"以后再说但没有边界"的表述**——§6.2、§6.6、§7.1、§10、§12 及两处里程碑，全部改为"触发条件（含数字阈值）"或"截止时间"；写不出边界的条目直接删除，不再保留"以后评估""视情况"这类空话。
> v1.4 变更：补齐**三个视图的布局规划**（§4.1 新增视图清单，配套 `preview.html`：首页 / 笔记详情页 / 照片灯箱）。此前只有首页有版式规划，另两个视图只有文字描述。
> v1.5 变更：**新增 §6.7「本期不做的东西，以后怎么加」**（四步变更流程 + 4 项的前置门槛 + 对 6.1 搜索框原因的补注）。本次只做新增，**未修改第 6 节任何已有结论**。
> v1.6 变更：**技术路线由"纯静态"改为"React + CloudBase 全栈"**（依据 `TECH_DESIGN.md` v1.0 §1）。本次只修改与该路线**直接冲突的 19 处表述**，并按 §6.7 的四步变更流程**新增 §4.10 与 §5.6**（把评论、热度、统计三项从 §6.2 移入范围）；**未重写任何章节、未删除任何已确认结论**。另：文档头版本号此前停留在 v1.2（v1.3 / v1.4 / v1.5 三次变更漏改版本号），本次一并修正为 v1.6。落笔前的原版已存档为 `PRD.v1.5.bak.md`。

---

## 1. 背景与目标

### 1.1 为什么做这件事

不是因为它解决了某个"痛点"，而是因为一个明确的愿望：**要一个属于自己的、样式完全自主的地方，用来长期存放我拍的照片、学习记录和日常感受。**

诚实说明（不编造痛点）：做完之后我不会放弃任何一个现有做法——朋友圈照发、相册照用。所以本项目的性质是**增量项目**，不是替代品。它的真正对手不是别的工具，而是"三周后再也没更新"。

### 1.2 一句话定位与首屏文案

| 位置 | 文案 | 字数 |
| --- | --- | --- |
| 站点名（品牌名） | **Ja的小屋** | 5 |
| 主标题（首屏最大字） | **Ja的小屋** | 5 |
| 定位语 | **存放着时间** | 5 |
| 副标题 | **从这里了解我** | 6 |

- 定位语"存放着时间"指**长期积累这个动作**，不意味着要把所有原图都放上网页（呈现方式是精选，见 4.3）。
- "存放"与"展示"的关系：**存放是目的，展示是方式**。
- 刻意**不把站点定义为"技术博客"或"生活博客"**——一旦写死，以后想加别的内容就会被自己的定位挡住。

### 1.3 目标用户与优先级

| 优先级 | 用户 | 他们进来后 10 秒内的第一个动作 |
| --- | --- | --- |
| **①** | **我自己** | 找最新的一条 |
| ② | 想了解我的人（**锚定人群：网上一起做项目的朋友**） | 先看图片 |
| ③ | 潜在 HR（次要，不作主要服务对象） | 找做过的事 / 看之前做过什么 |

**最高裁决标准**：当两个需求冲突时，选"取悦我自己"的那一个。

---

## 2. 使用场景

1. **我记录与回看**：写完一篇学习笔记或一条感悟，加进站点；过一阵回来按时间翻。
2. **朋友了解我**：我把链接发给他，他先看照片墙，再翻几条记录。
3. **HR 顺手看到**：从别处点进来，扫一眼照片与记录，判断"这人在做什么"——**由信息卡里的自我介绍承担**（见 4.2）。
4. **分享场景**：主要是**电脑**打开（锚定人群是项目伙伴，多在 GitHub / QQ / 微信 PC 端），手机上也会有朋友点开——所以移动端只需保证"不横向滚动"（见 5.4）。

---

## 3. 要解决的核心问题

**一句话**：这些内容现在散落在别人的平台里——照片躺在手机相册、感受发在朋友圈给一群不相干的人看——我缺一个**样式我说了算、长期属于我**的地方。

### 3.1 现有方案的具体缺陷（不编造）

- 发照片会有一堆**不相干的人**点赞评论；
- QQ 空间**功能太多**，界面全是别人的设计意图。

### 3.2 为什么不采用现成方案（Hexo / WordPress / Notion 公开页）

两条理由并列：
1. **想借这个项目学 vibe coding**——学技术本身就是目标之一；
2. **想要完全按自己心意的样式**——现成主题改不动、改起来比自己写还费劲。

### 3.3 隐私边界（硬约束）

**本节的约束范围是本站点对外的全部呈现**（页面文字、图片内容、访问机制）。它**不等于"不可被任何人关联到本人"**——后者在公开仓库 + 公开邮箱的前提下做不到（见 7.9）。

对**页面文字**：
- 不出现真实姓名、真实地址、**学校全名**；
- 页面上自称 **Jack**；
- 可写专业方向与年级，不写学校名；
- 若涉及他人信息，**先沟通取得同意再写**。

对**图片内容**（补充，v1.1 新增）：
- 头像即本人正脸，**照片墙与封面图也可能出现他人面孔**——这些属于"公开可见"，发布前须逐张自查：
  1. 是否有他人正脸且未获同意 → 换图或处理；
  2. 是否有门牌、车牌、快递单、工牌、宿舍号 → 换图或打码；
  3. 是否有可识别到具体地点的地标（校门、宿舍楼、常去店铺）→ **有就换图或裁掉该部分；纯风景、无明显地标可保留**。
- **图片元数据（EXIF）另见 7.1**，那是单独一项已知取舍。

对**访问机制**：
- 不做任何权限机制 —— **站点是公开的，任何放上去的内容都等于对所有人公开**。

---

## 4. 功能范围（这一版做什么）

### 4.1 页面与视图清单

**页面清单（v1.6：改为前端路由，不再是"每篇一个 HTML 文件"）**

| 页面 | 路径 | 说明 |
| --- | --- | --- |
| 首页 | `/`（React 单页应用） | 一屏到底 + 锚点导航 |
| 笔记详情页 | `/notes/:id` | 由数据驱动，**不再每篇一个文件** |
| 后台录入页（阶段二起） | `/admin` | 仅本人使用，需管理令牌；属工具页，说明见下 |

照片与感悟**都不单独成页**：照片在照片墙内放大；感悟在卡片上直接显示全文。

**视图清单（3 个视图，每个都有布局规划）**：

| # | 视图 | 落在哪 | 布局规划在哪看 | 生效阶段 |
| --- | --- | --- | --- | --- |
| 1 | **首页** | `/` | `preview.html` → 视图一 | 一 |
| 2 | **笔记详情页** | `/notes/:id` | `preview.html` → 视图二 | 一 |
| 3 | **照片灯箱**（浮层视图） | 由首页触发，不单独成页 | `preview.html` → 视图三 | 一 |

> `preview.html` 是视图规划文件，双击即可在浏览器打开；Day 7 实现时以它作为版式对照。三张视图里的灰色块均为图片占位（素材到位后替换），示例文字仅为展示版式，不是真实内容。
> **后台录入页（`/admin`）不列入视图清单**（v1.6 决定）：它是只有本人使用的工具页，不面向访客，因此不做版式规划。若日后认为它也需要版式规划，按 §6.7 的四步流程重新提案。

### 4.2 首页区块与顺序

```
顶栏（吸顶，毛玻璃）          首页 · 照片墙 · 记录 · 关于
背景大图 + 信息卡            整张背景图（模糊压暗）+ 一张毛玻璃信息卡
照片墙                       错落两列
记录卡片流                   笔记 + 感悟同一条流，按时间倒序，2 列
页脚                         站点名 · 建站年份 · 邮箱
```

**信息卡内容**（合并了原本的"关于我""站内概览""联系方式"三块）：

| 位置 | 内容 |
| --- | --- |
| 左 | 头像（方形圆角） |
| 右 | 主标题 `Ja的小屋` / 定位语 `存放着时间` / 副标题 `从这里了解我` / **一段 2–3 句的自我介绍**（专业方向 + 现在在做什么，不写真名与校名） |
| 底部 | `照片 N 张` · `笔记 N 篇` · `建站 N 天` + 两个圆形图标（GitHub、邮箱） |
| 右下小字 | `最后更新于 YYYY-MM-DD` |

**四个锚点的指向**（避免死链，v1.1 明确）：

| 导航项 | 锚点指向 |
| --- | --- |
| 首页 | 页面顶部（信息卡） |
| 照片墙 | 照片墙区块 |
| 记录 | 记录卡片流区块 |
| **关于** | **信息卡内的自我介绍段落**（"关于我"由信息卡承载；点它滚到该段并短暂高亮，避免"点了没反应"） |

**两个日期口径**（v1.1 明确定义）：

- **建站 N 天** = 从**首次上线日**起算到当天；
- **最后更新于 X** = 最近一次新增内容（照片/笔记/感悟均可）的日期；
- 两者均由 **AI 在每次加内容时同步修改**（见 4.7）——**此为阶段一的做法**。阶段二起，信息卡上的数字由接口实时计算，不再需要人工同步（见 4.7、5.2）。
- 补充：**"建站 N 天"不需要存进数据库**，它由"首次上线日 + 当天日期"在前端算出，只需改一处常量。

### 4.3 内容形态（四类 → 三类）

| 类型 | 说明 | 卡片显示 | 是否有详情页 |
| --- | --- | --- | --- |
| 照片 | 只能由本人提供；照片墙**纯图**，无标题、无说明 | — | ❌ 仅灯箱放大 |
| 笔记 | 学习记录（今天与之前学的） | 标题 + 日期 + 封面图（可选） | ✅ |
| 感悟 | 像跟朋友分享每一天的感受，**允许没有标题** | 一张配图 + 几句正文 | ❌ |

- 笔记与感悟**同一条卡片流**，按时间倒序，用标签区分（标签只做视觉区分，不做筛选）。
- 笔记**必须有标题**；感悟**允许无标题**。
- 日期 = **发布时间**。
- 长内容处理：感悟若超过约 150 字，**改写成笔记**发布。
- 正文内插图：最多 3 张，统一裁成 **4:3**。

**与上游 research.md 的有意偏离（v1.1 补记，非遗漏）**：

| 上游结论（research §9.1） | 本期做法 | 理由 |
| --- | --- | --- |
| 卡片含"一句摘要" | **不做摘要**，卡片只有标题 + 日期 + 封面 | A/B 轮决定：手写摘要增加写作负担，自动截断又难看；内容少时靠标题判断足够 |
| "关于我"独立区块 3–5 句 | **并入信息卡**（2–3 句自我介绍） | 参照物布局就是这样；同时减少首页长度 |
| 站内概览含"分类 N 个" | **去掉分类数字**（只留照片数、笔记数、建站天数） | 本期不做分类标签，就没有分类可数 |
| 卡片上有"阅读全文"入口 | **去掉该文字入口**，整张卡片可点 | 少一个元素更清爽；笔记卡本身已是入口（感悟卡不可点，见 5.2） |
| 封面图必含 | **改为可选** | 感受类内容常常没有合适的封面图，强制要求会拖住发布 |
| 每张卡片都能点进详情页 | **感悟卡不跳转**，直接在卡片上显示全文 | 感悟只有几句话，为它做一套详情页不划算；这条偏离同时改变了验收项（见 5.2） |

### 4.4 照片墙规则

- **错落两列**：照片比例近似但不完全统一，允许有高低差；不做"整屏铺满"。
- 点击 → **页面内灯箱**：可左右切换、点任意处关闭；JS 失效时仍可打开原图。
- 显示 **"共 N 张"**（与信息卡数字同步；**阶段一由 AI 修改，阶段二起由接口返回的实际条数**）。
- **增减的维护方式**（v1.6 按阶段区分）：
  - **阶段一**：由我定期手动维护（增删文件 + 改首页数字）；
  - **阶段二起**：在后台录入页 `/admin` 增删照片记录，前台自动更新，不需要改文件。
- **不做"内容变多自动分页"的逻辑**（两个阶段一致；照片数超过一屏时先靠手动取舍，分页的触发条件见 §10 的条目规则）。

### 4.5 详情页结构（默认值，可否决）

返回首页 → 封面图（**可选，无则省略**）→ 标题 + 日期 → 正文（文字 + 图，图统一 4:3）→ **上一篇 / 下一篇**（仅在同为笔记的内容之间跳转）。

### 4.6 视觉要求

- **深色背景大图（模糊压暗）+ 毛玻璃卡片** —— 这是"好看"的主要来源。
- 全站文字为浅色；一个主色 + 大量中性色。
- 首屏是一张图起手。
- **图片比例规则（v1.1 消歧）**：
  - **照片墙** → 按 4.4 的"错落两列"（比例近似，允许高低差）；
  - **笔记封面图、正文插图、详情页封面** → 统一比例：正文插图固定 **4:3**；封面图统一 **16:9**（v1.2 补定具体值）。
- 副标题所用的"最小一级文字"样式是全站基准，卡片标签、日期、页脚文字**统一沿用**（分隔符统一用 `·`）。

### 4.7 加内容的流程（谁动手）

| 步骤 | 阶段一（视觉壳上线前后） | 阶段二起（全栈就位后） |
| --- | --- | --- |
| 1. 挑图、裁图、压缩 | Jack（提供素材） | Jack（不变） |
| 2. 录入 | **AI 代办**：改文件、写详情页、更新卡片流与数字（照片数/笔记数/最后更新于），并同步给 Jack 一份"加内容操作卡" | **Jack 在 `/admin` 后台自行录入**，提交即生效，不打开代码、不执行提交 |
| 3. 长期目标 | Jack 逐步接手加内容 | 同左，**可判定标准不变：到第 5 篇内容时，Jack 能在不提问的情况下独立完成一次录入** |

**边界提醒（与 AGENTS.md 对齐）**：AI 代办**仅在当日任务清单授权范围内**执行；清单没写的事，先问再动。

**发布前必做自查**：隐私边界（3.3，含图片内容三条）→ 内容边界（6.5）→ 图片元数据按 **7.1 的已知取舍**处理（当前选择：不剥离；若日后决定剥离，用 Windows「属性 → 详细信息 → 删除属性和个人信息」）。

**（v1.6 补）** 阶段二起，"发布前自查"移到**后台提交之前**执行——入库即公开，没有"提交后再改"的缓冲（见 7.2）。

### 4.8 交付与上线（v1.1 新增）

| 项 | 内容 |
| --- | --- |
| 托管方式 | **CloudBase 静态网站托管**（页面与静态资源）+ **CloudBase 云函数**（后端）+ **CloudBase PostgreSQL**（数据）。Day 5 已定案，见 `TECH_DESIGN.md` §1 |
| 访问地址 | **待腾讯云环境创建后给出**（先用平台默认域名；自定义域名属"本期不做"，见 6.3） |
| 上线判定 | 别人在**自己电脑上**打开该地址能看到完整首页 = 已上线。**此判定与阶段无关**——阶段一（视觉壳）上线即可满足，后两阶段不改变这条 |
| 代码存放 | 与现有仓库同一仓库（`myself-block`）：前端、云函数、建表脚本都进这一仓库 |

### 4.9 里程碑与排期（v1.1 新增，按课程节奏）

| 阶段 | 内容 | 完成标志 |
| --- | --- | --- |
| Day 5 | 技术选型与工程约定 | **已完成**：`TECH_DESIGN.md` v1.0（全栈方案） |
| Day 6 | 视觉规范落地（配色、字号、间距） | 定稿一套视觉规范 |
| Day 7 | **MVP 实现（阶段一）**：首页 4 个区块 + 笔记详情页的视觉壳 | 本地能打开、内容就位 |
| **Day 7 当晚（最迟 Day 8）** | 上线并做成功线检查（**5.7**） | 别人可访问 + 我愿意发链接 |
| 阶段二（**未排期**，见 §12.7） | 建库 + 云函数 + 后台录入页（`TECH_DESIGN.md` §11） | 在 `/admin` 录入一条内容，刷新首页即出现 |
| 阶段三（**未排期**，见 §12.7） | 评论 + 阅读量 | 访客能提交评论并在页面上看到 |

> **阶段一必须先上线**——它保住"随时有链接可发"这个底线，后两阶段是增强而非重修。
> 阶段二、三**当前按"另排"落地**（不挤进课程节奏），前置条件是腾讯云环境创建（Jack 本人操作）。改判规则见 §12.7。

### 4.10 访客互动与全栈能力（v1.6 新增，阶段二 / 三生效）

| 能力 | 内容 | 生效阶段 |
| --- | --- | --- |
| 内容录入 | 本人在后台录入页 `/admin` 填写标题、正文、类型与图片，提交即生效；信息卡数字自动准确 | 二 |
| 评论与留言 | 访客在笔记详情页提交昵称与内容；显示策略已按 6.2 的①落地 | 三 |
| 热度 / 阅读量 | 记录并展示内容被阅读的次数 | 三 |
| 访问统计 | 后台可查看访问量数字 | 三 |

> 以上四项此前列在 §6.2「本期不做」，2026-09-24 决定采用全栈方案后移入范围。按 §6.7 的四步变更流程执行：**从 §6.2 移出，同时写入本节（§4）与 §5.6（验收）**。用户登录注册、分类筛选、站内搜索**仍不做**。

---

## 5. 验收标准（怎么算做完）

### 5.1 内容

- 照片墙：**至少 3 张、目标 6 张**（低于 3 张不上线）。
  - 与上游 research §9.1 的"6–9 张"口径不同：**这是有意下调**，目的是避免因"挑不出第 6 张满意的图"而卡住上线；6 张仍是目标值。
- 笔记：**至少 3 篇**，每篇能在详情页正常阅读。
- 感悟：**至少 1 条**（v1.2 补。理由：卡片流里对感悟卡有验收项，若一条感悟都没有，那几项验收既不能判通过也不能判不通过）。
- 笔记主题建议：学习 / 生活 / 随想各一篇（三篇即可证明"内容可混主题"）。

### 5.2 功能（每条均可判通过/不通过）

| 项 | 判定方法 |
| --- | --- |
| 照片能放大 | 点击照片墙任意一张 → 出现全屏大图；按左右方向可切换；点图外区域可关闭 |
| 笔记能进详情页 | 点击任意一张笔记卡片 → 打开对应详情页；详情页有返回首页的链接且能点回 |
| 详情页上下篇可用 | 详情页底部的上一篇/下一篇能点开对应笔记（第一篇无上一篇、最后一篇无下一篇） |
| 联系方式可点 | 点信息卡上的邮箱图标 → 唤起邮件客户端；点 GitHub 图标 → 打开仓库页 |
| 无死链 | 全站所有可点击元素逐个点一遍，均能到达目标（含导航 4 项） |
| 无坏图 | 页面滚动到底，没有任何图片显示为破图或空白（加载失败时应有 alt 文字） |
| 数字与实际一致 | **阶段一**：信息卡上的"照片 N 张"= `public/assets/photos/` 里的实际图片数；"笔记 N 篇"= 卡片流中笔记卡的实际条数；"最后更新于 X" = 最近一次新增内容的日期。**阶段二起**：同样三个数字，判定口径改为"= 数据库 `photos` 表 / `posts` 表中 `type='note'` 的条数"，"最后更新于" = `posts` 表中最大的 `published_at`。**两个阶段都必须满足**，只是"数数的对象"不同 |
| 感悟卡不可点、笔记卡可点 | 点感悟卡片任何位置不发生跳转；点笔记卡片任意位置进入其详情页 |

### 5.3 视觉（可判定化，v1.2 修订）

| 原表述 | 判定方法 |
| --- | --- |
| "3 秒内读懂这是谁的空间" | **在首屏不滚动的前提下，主标题与定位语同时可见**；副标题也在首屏内 |
| "全站风格一致" | 抽查首页与任意详情页：**卡片圆角、正文字号、配色、标签样式**沿用同一套。注意：**笔记卡与感悟卡构成不同（一个有标题、一个没有）不算"风格不一致"**，本项只判"同一类元素是否用了同一套样式" |
| "首屏有视觉起点" | 首屏可见背景大图（不是纯色背景） |

### 5.4 兼容

- **桌面优先**：在 **1280×720 的浏览器窗口**下打开，**不出现横向滚动条、元素不重叠**（第一版验收以此为准）。
- 移动端**只保证一件事**：手机上打开**不出现横向滚动**；其余排版问题不列入验收。

### 5.5 隐私与内容边界（可判定，v1.2 新增）

这一项放在成功线之前，因为它是硬约束（3.3）唯一能被拦截的关口——一旦提交，就删不干净（7.2）。

| 项 | 判定方法 | 执行人 |
| --- | --- | --- |
| 图片逐张自查 | 上线前把 `public/assets/` 里的每张图过一遍 3.3 的三条（他人正脸 / 门牌车牌快递单 / 可定位街景），逐张记录"通过 / 已换图 / 已处理" | **Jack** 确认；AI 提供逐张清单 |
| 页面文字自查 | 通读首页与全部详情页，确认不出现真实姓名、真实地址、学校全名 | Jack 确认 |
| 内容边界自查 | 对照 6.5 的四类（家里的事 / 成绩 / 朋友私事 / 实时位置），确认没有出现 | Jack 确认 |
| 留证方式 | **阶段一**：每次加内容或提交前，AI 在文件清单里附一行"隐私自查：3 条已确认"，由 Jack 回一句确认后才提交。**阶段二起**：改为在后台提交按钮旁列出 3.3 的三条自查项，**提交前必须逐项打勾** | 阶段一：AI 出清单 + Jack 确认；阶段二起：Jack 在后台逐项勾选 |

### 5.6 全栈功能（阶段二 / 三起生效，v1.6 新增）

| 项 | 判定方法 | 生效阶段 |
| --- | --- | --- |
| **录入即生效** | 在 `/admin` 提交一条内容 → 刷新首页，新内容出现在卡片流首位，且信息卡数字同步 +1 | 二 |
| **录入无需改代码** | 整个录入过程不打开任何代码文件、不执行 git 提交 | 二 |
| **接口异常有降级** | 断开后端（或故意填错数据库连接串）后打开首页：静态骨架仍在（顶栏 / 背景图 / 信息卡框架），数据区块各自显示"加载失败，点此重试"，**不整页白屏** | 二 |
| **访客能提交评论** | 访客在某篇笔记的详情页填写昵称与内容 → 提交成功，并按 6.2 确定的显示策略出现在页面上 | 三 |
| **统计数字可见** | 后台能看到阅读量 / 访问量数字 | 三 |

### 5.7 成功线（真正的那一条）

- 以上都过了，才轮到这条：**我愿意主动把这个链接发给别人。**

---

## 6. 本期不做（砍掉了什么、为什么砍）

### 6.1 参照物有、但我们不做

| 不做的事 | 为什么砍 |
| --- | --- |
| 搜索框 | 需要索引服务；只有 3 篇内容时搜不出东西，**放一个搜不到的搜索框比不放更伤体验** |
| 音乐播放器 | 需要音频文件与版权，还会拖慢首屏 |
| 悬浮角色挂件、侧边按钮、九宫格菜单 | 装饰性 JS，收益低于维护成本 |
| 十项导航 | 我们只有 1 页 + 详情页，10 项会全是死链（精简为 4 项） |

### 6.2 需要后端的

**（v1.6 变更）** 本节原有六项，其中**三项已移出本节、改为要做**：

| 原列项 | 现状 | 现在写在哪 |
| --- | --- | --- |
| 评论与留言 | **移出本节，改为阶段三要做** | §4.10、§5.6 |
| 热度 / 阅读量 | **移出本节，改为阶段三要做** | §4.10、§5.6 |
| 访问统计 | **移出本节，改为阶段三要做** | §4.10、§5.6 |

**仍在"本期不做"的三项**：用户登录注册 · 分类筛选 · 站内搜索 —— 一律需要服务端或数据库，**继续不做**。什么时候才可能做：第 10 节给了每一条的触发条件（含数字阈值），条件未满足前不做、也不提议。

**评论显示策略（v1.6 暂按①落笔，可按 §12.8 改）**：

| 选项 | 做法 | 代价 |
| --- | --- | --- |
| **① 先审后显**（当前采用） | 评论入库标 `pending`，本人在后台点"通过"才可见 | 多一步人工操作；朋友可能觉得"发了没显示" |
| ② 直接显示 | 提交即公开 | 省事；有被垃圾评论刷屏的风险（缓解手段：接口限流 + 关键词过滤） |

> 当前按 **①** 落地，与 `TECH_DESIGN.md` §14-Q2 的建议一致。**改为②的成本**：数据库 `comments.status` 的默认值由 `pending` 改为 `visible`，并去掉后台的审核列表——实现量很小，切换时机见 §12.8。

### 6.3 工程类

**（v1.6 变更）** 本节原有八项，其中**三项移出"不做"**：

| 原列项 | 现状 |
| --- | --- |
| 后台管理系统 | **阶段二要做**：简化为一个 `/admin` 录入页，不是完整 CMS |
| 图床 / 对象存储 | **阶段二要做**：图片改走云存储（对应 `TECH_DESIGN.md` §14-Q1） |
| Markdown 自动渲染引擎 | **阶段二视录入方式而定**：录入用 Markdown 就需要；用纯文本 / 富文本就不需要。原"手写 HTML 详情页"的做法已随全栈方案失效 |

**继续不做**：云服务器（用云函数，不租服务器）· 自定义域名与 HTTPS 证书（先用平台默认地址）· 多语言 · 邮件订阅 / RSS · 小程序或 App 版本。

### 6.4 内容类型

**链接收藏 / 友链**——少一个类型就少一套模板，等真有想分享的链接再加。

### 6.5 内容边界（永不上线的四类）

家里的事 · 挂科或成绩 · 和朋友的私事 · 实时位置。

### 6.6 "做过的事（项目）"区块：本期不做，**第二版第一优先**

原因是我目前**还没有可展示的项目**。这个需求本身成立（目标用户里的 HR 想看），但素材不存在，硬做会得到一个空区块。**触发条件：出现第 1 个可公开链接的项目（比赛、课程作业、开源仓库均可）时启用**；在此之前，"做过的事"不出现在导航与页面里，也不做占位区块。启用后位置放在记录卡片流之后。

### 6.7 本期不做的东西，以后怎么加（v1.5 新增）

**先澄清一件事**：本节只说明"以后想加时的正规路径"，**不修改第 6 节已有的任何结论**——上面所有条目，第一版一律不做。

**四步变更流程**

| 步 | 做什么 | 产出 |
| --- | --- | --- |
| 1 | **归属判断**：属于第 10 节的候选（看触发条件），还是属于本节被砍的项（走下面第 2 步） | 一个判断 |
| 2 | **重新提案**：想加什么 / 为什么现在值得加（新事实或触发条件满足）/ 代价（工期、维护、体积、隐私）/ 从哪一版开始 | 一段提案文字 |
| 3 | **落地两处**：通过后从第 6 节移出，**同时写进第 4 节（功能范围）和第 5 节（验收标准）**——只写"要做"不写"怎么验收"，等于没加；版本号 +0.1 | PRD 两处修改 |
| 4 | **提交留痕**：`Day X｜PRD 变更：新增 XXX（理由一句话）` | 一条可追溯的提交 |

**4 项被砍条目的前置门槛（满足才重新提案）**

| 项 | 前置门槛 | 通过后的代价 |
| --- | --- | --- |
| 搜索框 | 内容 **≥20 条**，并接受引入一个轻量前端搜索库 | 多一个 JS 依赖；每次加内容要更新索引文件 |
| 音乐播放器 | 有**合法来源**的音频（自己录的或免版权）；接受首屏体积上升 | 版权风险；加载变慢 |
| 悬浮挂件 / 九宫格菜单 | 必须绑定**明确用途**（如"回到顶部"），纯装饰不做 | 维护成本 |
| 十项导航 | 页面数 **≥6**（每项都有真实落点） | — |

**对 6.1 中"搜索框"一格的补充说明（原文保留不改）**：6.1 写的是"需要索引服务"，这个说法偏保守——不引入后端也能做搜索（前端搜索库 + 一个生成的索引文件）。因此搜索框的**真实门槛是内容量（≥20 条）**，而不是"必须上后端"。原文不动，正式口径以本注为准。

> **v1.6 执行记录**：本次（评论与统计移入范围）即按上述四步流程执行——第 1 步归属判断为"§6.2 被砍项"；第 2 步因 Day 5 决定采用全栈方案而成立；第 3 步已从 §6.2 移出并写入 §4.10 与 §5.6，版本号 v1.5 → v1.6；第 4 步提交留痕见仓库提交记录。

---

## 7. 风险与已知取舍

| # | 风险 / 取舍 | 现状与后果 |
| --- | --- | --- |
| 7.1 | **图片元数据（EXIF）** | 手机照片含拍摄地点经纬度等元数据；拷贝到电脑再上传**不会**去掉。上传后**任何人可下载原图读出拍摄地点**。当前选择：**不剥离**（知情决定）。例外处理规则：**若某张照片的拍摄地点属于"我常住或常去"的范围，仅对该张剥离元数据**（用 Windows「属性 → 详细信息 → 删除属性和个人信息」）。**与 3.3 的关系**：3.3 约束的是"对外呈现"（含图片内容与页面文字），本条是它之下**一项被明确豁免的例外**——即"页面不写地址"成立，但"原图元数据不含地址"不成立。另注：转 WebP 时元数据是否被丢弃，**在 Day 6 处理图片时实测一张确认并把结果写回本条**，未实测前不假设它已消失 |
| 7.2 | **内容删不干净** | 一旦提交过，git 历史里仍有记录，删文件不等于消失。因此坚持"发布前自查"，不做事后补救 |
| 7.3 | **不承诺更新节奏** | 项目最大风险是"上线即荒废"。本文不设更新承诺，只保证"结构支持随时增加" |
| 7.4 | **桌面优先** | 手机上可能出现排版挤、照片墙变单列等观感问题（只保证不横向滚动） |
| 7.5 | **多处手工同步**（**仅阶段一存在**） | 阶段一：照片数、笔记数、"最后更新于"、照片墙增减都需手动改；忘记同步会让页面数字与实际不符。**阶段二起本风险消失**——数字由接口实时计算（`TECH_DESIGN.md` §7.2 第 5 步），取而代之的是新风险"接口故障导致数据区块不显示"，另有应对（`TECH_DESIGN.md` §8） |
| 7.6 | **感悟无详情页** | 感悟只能写短的（约 150 字内），写长了必须改写成笔记 |
| 7.7 | **私有内容无处安放** | 站点全公开，没有"只给朋友看"的机制；这类内容只能不发 |
| 7.8 | **肖像与版权** | 只用本人拍摄或已获授权的图片；头像即本人正脸、照片可能含他人面孔，须按 3.3 逐张自查 |
| 7.9 | **身份可被反向关联**（v1.1 新增） | 公开仓库账号（JIMIHA-WU）、公开邮箱、GitHub 主页三者叠加，**别人可以据此关联到本人真实身份**。因此 3.3 的承诺是"页面上不写姓名与校名"，**不是"无法被找到"**。若需更强的匿名性，只能换账号 + 仓库转私有（与"公开可访问"目标冲突，本期不做） |
| 7.10 | **主观项验收偏差**（v1.1 新增） | "好看"最终靠本人判断，5.3 只是把主观项转成可判定动作，不代表别人也会觉得好看 |
| 7.11 | **后端故障面**（v1.6 新增） | 引入云函数与数据库后，"站点能不能打开、内容能不能显示"不再只取决于静态文件。应对：分区块降级（任何后端故障不得导致整页白屏，见 `TECH_DESIGN.md` §8）；密钥保管与备份另见 `TECH_DESIGN.md` §9 |

---

## 8. 内容存放与命名规范

参照物的做法（实际观察）：图片不与页面混放，按用途分目录，命名机器友好（LX Blog 用 `/image/bg/Landscape/01.webp` 这类"类型目录 + 序号"；知遥用构建产物哈希命名）。我们照此思路。

**（v1.6 变更：文字内容不再是一个个 HTML 文件，而是数据库记录；图片仍是文件，命名规则不变。）**

| 类型 | 存哪 | 谁负责编号 |
| --- | --- | --- |
| 文字（笔记 / 感悟的标题、正文、日期） | 数据库 `posts` 表（`TECH_DESIGN.md` §5） | 数据库自增 `id`，**不需要手工编号** |
| 图片（照片 / 封面 / 插图 / 头像 / 背景） | 仍是文件，仍是下面这套目录与命名 | 沿用编号规则，**规则不变** |

```
myself-block/
├─ client/                             前端（React + Vite）
│   └─ src/                            页面组件与样式
├─ server/                             云函数（后端，只有它能访问数据库）
│   └─ functions/                      接口实现
├─ db/                                 建表脚本
│   └─ schema.sql
├─ public/assets/                      静态资源（构建后由托管发出）
│   ├─ photos/                         照片墙（错落两列用）
│   │   └─ photo-01.webp  photo-02.webp …
│   ├─ covers/                         笔记封面（与笔记记录对应）
│   │   └─ 2026-09-20-xxx.webp
│   ├─ inline/                         笔记正文插图（每篇最多 3 张）
│   │   └─ 2026-09-20-xxx-1.webp  -2.webp  -3.webp
│   ├─ avatar.webp                     信息卡头像
│   └─ bg.webp                         首屏背景大图
├─ docs/                               PRD.md  research.md  TECH_DESIGN.md  things.md  preview.html
└─ AGENTS.md  .gitignore
```

- 照片墙：`photo-NN.webp`，两位序号**按展示顺序**编号（换顺序就改名，不改代码逻辑）。
- 封面：`YYYY-MM-DD-短名.webp`，与笔记记录**一一对应**（用日期 + 短名作为关联键；详情页已不再是文件）。
- 正文插图：`YYYY-MM-DD-短名-序号.webp`（序号 1–3），放在 `public/assets/inline/`，与所属笔记同名便于配对。
- 感悟配图：沿用 `public/assets/covers/`，命名 `YYYY-MM-DD-短名.webp`（与那条感悟同日期），不单独开目录。
- **短名用中性词**（如 `note-a`、`daily-01`），避免出现课程名、实验室名等可反推身份的线索。
- 头像/背景：固定用 `avatar.webp` / `bg.webp`。
- 格式优先 **WebP**（体积小），其次压缩后的 JPG。

---

## 9. 默认值清单（我替你定的工程细节，可一键否决）

| # | 项目 | 默认值 |
| --- | --- | --- |
| 9.1 | 详情页结构 | 返回首页链接 + 封面（**可选，无则省略**）+ 标题 + 日期 + 正文 + 上下篇 |
| 9.2 | 页脚 | 站点名 + 建站年份 + 邮箱 |
| 9.3 | 图片加载失败 | 显示占位底色 + alt 文字 |
| 9.4 | 毛玻璃降级 | 浏览器不支持时退化为纯半透明底色 |
| 9.5 | 图片体积上限 | **照片与封面：单张 ≤ 300KB；首屏背景大图放宽到 ≤ 600KB**（v1.1 修订，避免背景被压糊） |
| 9.6 | 浏览器范围 | Chrome / Edge / Safari 近两年版本 |
| 9.7 | 桌面最小宽度 | 1280px |
| 9.8 | 详情页上一篇/下一篇 | 仅在同为笔记的内容之间跳转 |

---

## 10. 后续候选（第二版及以后）

**本节规则：每条必须带"触发条件"（可判定的数字或事件）；写不出触发条件的条目不放进来。** 触发条件未满足前，不做、也不提议。

| 候选 | 触发条件（满足才启动） |
| --- | --- |
| "做过的事（项目）"区块 | 出现**第 1 个**可公开链接的项目（比赛、课程作业、开源仓库均可） |
| 移动端精细适配 | 收到 **≥2 位**朋友明确反馈"手机上看不清" |
| 分类筛选 / 搜索 | 内容 **≥20 条**，且手工翻找一条旧内容单次耗时 **>2 分钟** |
| ~~评论与留言~~ | **已移出候选**：2026-09-24 决定采用全栈方案，评论进入阶段三（见 §4.10、§5.6、§6.2） |
| Markdown 化内容管理 | 连续 **2 次**加内容耗时 **>20 分钟** |

（这 4 条触发条件由我持续对照维护；满足时我主动提醒，未满足时不提。）

---

## 11. 公开参考资料

| 来源 | 用途 |
| --- | --- |
| https://www.xinghuisama.top/about | 最早的参照物；本次**首页布局**（背景大图 + 信息卡 + 卡片流）主要参照它 |
| https://blog.lin-xin.top | 卡片流骨架、站内概览数字、图片目录组织方式 |
| https://imadu.cn/ | 技术/生活内容混排的证据；热度评论等后端能力的反例 |
| research.md（本项目） | 同类产品比较、MVP 范围与"本期不做"的来源 |

---

## 12. 不确定与待核实

1. **实际观感未经真人验证**：毛玻璃、错落两列等视觉判断尚未给别人看过。**处理规则：上线后交给 ≥2 位朋友试看；若 2 人都反馈"看不出重点"或"排版乱"，则回到 5.3 调整；在此之前不改视觉。**
2. **工期为估算**：按课程节奏（Day 7 出 MVP）推导，实际耗时未测。**处理规则：Day 7 每个时段结束在 `things.md` 记一次实际耗时；累计偏差超过 50% 就当天启用方案 B 砍项。**
3. **照片与封面素材尚未到位**：拍摄、挑选、裁切由本人完成。**截止时间：Day 6 结束前（对应 `things.md` 的 C1–C3）；若届时仍缺 1 篇笔记，Day 7 按方案 B 砍项，不延期。**
4. **手工同步容易漏**（**仅阶段一**）：数字与实际内容的一致性靠操作卡提示。**处理规则：阶段一每次加内容必须走 `things.md` 的加内容流程，并逐项核对 5.2 的"数字与实际一致"；连续 2 次漏改，则把该步骤提到操作卡第一条。阶段二起本条自动失效**（数字由数据库生成，见 7.5）。
5. **第 9 节默认值未经逐条确认**：**处理规则：Day 6 开始实现之前，由我逐条复述这 8 项，Jack 未提出反对即视为生效；反对的按编号改。**
6. **托管与访问地址**（**v1.6 部分已定**）：**托管方式已于 Day 5 定案——CloudBase 静态托管 + 云函数 + PostgreSQL**（见 4.8、`TECH_DESIGN.md` §1）。**具体访问地址仍未定**，前置条件是腾讯云环境创建（本人操作）。**新的截止时间：阶段一上线前（Day 7 当晚，最迟 Day 8）给出平台默认地址**；自定义域名本期不做。
7. **阶段二 / 三的排期**（v1.6 已暂定，可改）：**当前按"阶段一先上线，阶段二/三另排"落地**（见 4.9）。**改判规则：阶段一上线（Day 7 当晚，最迟 Day 8）之后一周内，由 Jack 指定阶段二的起始时间；届时不指定，则默认搁置，本 PRD 不再主动提议。**
8. **评论显示策略**（v1.6 已暂定，可改）：**当前按"先审后显"落地**（见 6.2）。**改判规则：阶段三开始前由 Jack 确认一次；若届时未表态，则按当前值（先审后显）实现，之后不再作为待定项。** 改为"直接显示"的成本见 6.2 表下的说明。
